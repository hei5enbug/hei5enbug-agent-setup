interface:
  display_name: "Suggest Commit"
  short_description: "Recommend five evidence-backed repository-style commit subjects"
  default_prompt: "Use $suggest-commit to inspect all staged and unstaged changes, or the scope I name, and recommend five messages matching this repository history."
